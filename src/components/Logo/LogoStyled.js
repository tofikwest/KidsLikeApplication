import styled from "styled-components";

const LogoStyled = styled.div`
  display: flex;
  align-items: center;
  .logoTitle {
    margin-right: 8px;
  }
  .logoImg {
    width: 18px;
    height: 18px;
    fill: orange;
  }
`;

export default LogoStyled;
